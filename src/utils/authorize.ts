import Route from "@dylanbulmer/openapi/types/Route";

/**
 * @TODO Finish authorize function.
 * To be used as a express middleware to block unauthorized requests
 * and to refresh JWT tokens, iff necessary.
 */
const authorize: Route.Operation = (req, res, next) => {};
